/**
 * The primary entry point for this package is not used. A simple hello world function
 * is here as a place-holder.
 *
 * @returns {string}
 */

module.exports = (context, callback) => {
  callback(null, "hello world");
};
